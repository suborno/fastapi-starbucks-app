apiVersion: v1
kind: Service
metadata:
  name: starbucks-fastapi-service
  labels:
    app: starbucks-fastapi
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: starbucks-fastapi
